import React from 'react';

import { Box } from '@/components';

import styles from './styles.module.scss';

const Login = () => {
    return <Box className={styles.wrapper}>Login</Box>;
};

export default Login;
