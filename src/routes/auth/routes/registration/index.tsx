import React from 'react';

import { Box } from '@/components';

import styles from './styles.module.scss';

const Registration = () => {
    return <Box className={styles.wrapper}>Registration</Box>;
};

export default Registration;
